#!/bin/bash
# tp-jupyter — lance JupyterLab dans l'environnement Simu5G (port 8888, sans mot de passe)
DIR=/tp; [ -d "$DIR" ] || DIR=$HOME
exec ~/bin/tp-env "jupyter lab --ip=0.0.0.0 --port=8888 --no-browser --ServerApp.token='' --ServerApp.password='' --notebook-dir=$DIR"
